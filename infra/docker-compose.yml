version: '3.3'
services:

  db:
    image: postgres:13.0-alpine
    volumes:
      - /var/lib/postgresql/data/
    env_file:
      - ./.env
    ports:
      - '5432:5432'
    restart: always

  backend:
    image: aleksander777888/foodgarm-backend:latest
    volumes:
      - ../static/:/app/static/
      - ../media/:/app/media/recipes/
      - redoc:/app/api/docs/
    depends_on:
      - db
    env_file:
      - ./.env
    restart: always

  frontend:
    image: aleksander777888/foodgarm-frontend:latest
    volumes:
      - ../frontend/:/app/result_build/
    depends_on:
      - db

  nginx:
    image: nginx:1.19.3
    ports:
      - '80:80'
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/:/usr/share/nginx/html/api/docs/
      - ../static/:/var/html/static/
      - ../media/:/var/html/media/recipes/
    depends_on:
      - backend
      - frontend

volumes:
  static_value:
  media_value:
  postgres_data: